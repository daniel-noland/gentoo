_OPTIMIZE_FLAGS="-O3"

CFLAGS="${CFLAGS} ${_OPTIMIZE_FLAGS}"
CXXFLAGS="${CXXFLAGS} ${_OPTIMIZE_FLAGS}"
FCFLAGS="${FCFLAGS} ${_OPTIMIZE_FLAGS}"
FFLAGS="${FFLAGS} ${_OPTIMIZE_FLAGS}"

LDFLAGS="${LDFLAGS} -Wl,-O2"
LDFLAGS="${LDFLAGS} -Wl,--as-needed"
