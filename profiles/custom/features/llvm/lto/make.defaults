_LTO_OPTIMIZE_FLAGS="-flto=thin"
CFLAGS="${CFLAGS} ${_LTO_OPTIMIZE_FLAGS}"
CXXFLAGS="${CXXFLAGS} ${_LTO_OPTIMIZE_FLAGS}"
FCFLAGS="${FCFLAGS} ${_LTO_OPTIMIZE_FLAGS}"
FFLAGS="${FFLAGS} ${_LTO_OPTIMIZE_FLAGS}"
